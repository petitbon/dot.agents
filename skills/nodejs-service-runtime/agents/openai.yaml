interface:
  display_name: "Node.js Service Runtime"
  short_description: "Improve service runtime and operability."
  default_prompt: "Use $nodejs-service-runtime to improve Node.js service bootstrap, config, errors, observability, Cloud Run posture, and tests."

policy:
  allow_implicit_invocation: true
