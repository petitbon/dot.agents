interface:
  display_name: "Domain Event Architecture"
  short_description: "Model ownership, boundaries, and events when primary."
  default_prompt: "Use $domain-event-architecture when ownership, bounded contexts, contracts, event flows, or target-state architecture are the primary artifacts."

policy:
  allow_implicit_invocation: false
