interface:
  display_name: "Agentis Engineering Doctrine"
  short_description: "Govern target-state design and safe cutovers."
  default_prompt: "Use $agentis-engineering-doctrine when the task needs proportional, simple, fail-close target-state engineering or maintenance-window cutover planning."

policy:
  allow_implicit_invocation: true
