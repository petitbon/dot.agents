interface:
  display_name: "Agentis Engineering Doctrine"
  short_description: "Apply fail-close engineering posture when directly needed."
  default_prompt: "Use $agentis-engineering-doctrine when the task directly needs Agentis fail-close, fail-hard, simple target-state engineering posture."

policy:
  allow_implicit_invocation: false
