interface:
  display_name: "Agentis Engineering Doctrine"
  short_description: "Prevent overengineering and premature optimization."
  default_prompt: "Use $agentis-engineering-doctrine when the task needs proportional, simple, fail-close target-state engineering without premature optimization."

policy:
  allow_implicit_invocation: true
