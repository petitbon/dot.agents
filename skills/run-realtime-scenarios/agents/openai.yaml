interface:
  display_name: "Run Realtime Scenarios"
  short_description: "Run, evidence, and record realtime scenarios."
  default_prompt: "Use $run-realtime-scenarios to execute and evidence a governed Agentis phone or browser-chat scenario in dev, then record the latest outcome."

policy:
  allow_implicit_invocation: true
