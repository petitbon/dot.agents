interface:
  display_name: "Node.js Service Structure"
  short_description: "Shape service folders and dependency flow when primary."
  default_prompt: "Use $nodejs-service-structure when Node.js service layout, naming, layers, and dependency direction are the primary artifacts."

policy:
  allow_implicit_invocation: false
