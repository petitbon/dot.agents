interface:
  display_name: "Pagoda Framework"
  short_description: "Map Workbench EDD without platform coupling."
  default_prompt: "Use $pagoda-framework to interpret this Workbench storm, EDD registry, or outcome run and produce evidence-map, contract, oracle, or implementation guidance while keeping Pagoda testing artifacts owned only by agentis-pagoda-workbench."

policy:
  allow_implicit_invocation: true
