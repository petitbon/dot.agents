interface:
  display_name: "Pagoda Framework"
  short_description: "Workbench E2E evidence proof."
  default_prompt: "Use $pagoda-framework for Workbench-owned E2E outcome proof with Evidence/Outcome/Trace Contracts, Scenario Oracles, EDD runs, evidence traces, result classification, and Workbench/platform isolation."

policy:
  allow_implicit_invocation: true
